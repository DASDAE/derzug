name: derzug
channels:
  - conda-forge
dependencies:
  - python>=3.13
  - dascore>=0.1.15
  - duckdb
  - orange3
  - pyqt6
  - pyqtgraph
  - typer
  - coverage>=7.4,<8
  - prek
  - pytest
  - pytest-codspeed>=5,<6
  - pytest-cov>=4
  - pytest-qt
  # Conductor MCP server (streamable HTTP needs >=1.8; <2 before breaking v2)
  - mcp>=1.27,<2
  - pip
